# Local GitHub token for release-cache generation (do not commit)
# Create a fine-grained PAT with read-only access to required repos.
GITHUB_TOKEN=<github_token>
NEXT_PUBLIC_AUTH_BASE_URL=https://auth.subwaybuildermodded.com
